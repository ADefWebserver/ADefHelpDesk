/* Defines the DTOFileParameter entity */
export interface IDTOFileParameter {
	taskId: number;
        portalId: number;
        ticketPassword: string;
        detailId: number;
        attachmentID: number;
        emailFileName?: string;
}